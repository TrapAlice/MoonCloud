#pragma once

enum MESSAGE_IDS{
	DISCONNECT = 1,
	JOB_REQUEST,
	JOB_RESPONSE,
	JOB_ID,
	JOB_DATA,
	JOB_START,
	SET_STATUS_ACTIVE,
	SET_STATUS_IDLE,
	SET_STATUS_BUSY,
	GET_IDLE_NODE,
	CLOSE = 100,
	NODE_STATUS,
};